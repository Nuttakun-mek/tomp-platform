node_modules
.expo
apps/mobile-driver/.expo
.next
.next-check
.vercel
.env
.env.local
.env.*.local
dist
build
coverage
.DS_Store
*.log
*.tsbuildinfo

# Generated by scripts/live-test-smoke.mjs
scripts/live-test-qr.png
scripts/live-test-failure.png
scripts/mission-control-pilot.png
scripts/shot-*.png
scripts/audit/
_*.mjs
scripts/audit2/
scripts/audit3/

# seed output from scripts/seed-test-users.mjs (contains cloud ids, gitignored)
scripts/.seed-test-users.json

# Playwright
test-results/
playwright-report/
/e2e/.cache/

# mobile shell simulation artefact
mobile-shell-sim.png

# FCM / Google service-account keys — these are real secrets, never commit
*service-account*.json
tomp-project-*.json
fcm-key*.json
apps/mobile-driver/credentials.json

# Vercel env pulls — these hold the real service-role key, DB password and
# driver token secret. `vercel env pull` writes them without asking.
.env.vercel*
.vercel
