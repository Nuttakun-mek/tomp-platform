import { PageHeader } from "@/components/page-header";

export default function ResourcesPage() {
  return (
    <PageHeader
      eyebrow="Prepare"
      title="Resources"
      description="Drivers, vehicles, and call signs will be prepared here after the project and mission kernel exists."
    />
  );
}
