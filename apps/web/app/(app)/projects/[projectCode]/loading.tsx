// Shown below the project bar while a tab's server render runs, so a click
// answers at once instead of leaving the old page up with no sign of progress.
export { default } from "@/app/(app)/loading";
