import { PageHeader } from "@/components/page-header";

export default function ProjectsPage() {
  return (
    <PageHeader
      eyebrow="Planning"
      title="Projects"
      description="Project container workspace placeholder. Database-backed project flows begin in Sprint 1."
    />
  );
}
