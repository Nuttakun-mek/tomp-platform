import { PageHeader } from "@/components/page-header";

export default function DriverPage() {
  return (
    <PageHeader
      eyebrow="Field access"
      title="Driver"
      description="QR-based driver access placeholder. This route does not create tokens or read assignments yet."
    />
  );
}
