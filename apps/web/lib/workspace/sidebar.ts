// Read by the server shell and written by the client toggle. It lives outside
// the "use client" module: imported from there, a server component receives a
// client reference instead of the string.
export const SIDEBAR_COOKIE = "tomp_sidebar";
