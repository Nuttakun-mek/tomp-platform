export {
  BRIDGE_NAMESPACE,
  BRIDGE_VERSION,
  buildNativeStatusMessage,
  buildViewSwitchMessage,
  parseBridgeMessage,
  VIEW_SWITCH_EVENT
} from "@tomp/driver-core";

export type { BridgeMessage, NativeStatus, NativeStatusMessage } from "@tomp/driver-core";
