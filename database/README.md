# Database

Supabase database artifacts live here.

## Structure

- `migrations`: versioned schema changes.
- `policies`: Row Level Security policy documentation.
- `seed`: local and demo seed data.

Postgres is the operational source of truth. Timeline, audit, and post-publish change events are mandatory parts of the data model.
